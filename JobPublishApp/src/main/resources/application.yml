server:
  port: 7000
spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/jobapp_jobPostService
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: restaurant-service